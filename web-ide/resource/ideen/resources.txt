
name.martingeisse.webide.resources.handle:

IResourceHandle
	getMountSpace()
	getWorkspaceRoot()
	getProject()
	getParent()

IResourceContainerHandle : IResourceHandle
	getChildren()
	getChildren(type)
	getChild(name)

IFileContainerHandle : IResourceContainerHandle
	getFiles()
	getFile(name)
	getFolders()
	getFolder(name)

---

IMountSpaceHandle : IResourceContainerHandle
	getWorkspaceRoots()

IWorkspaceRootHandle : IResourceContainerHandle
	getProjects()

IProjectHandle : IFileContainerHandle

IFolderHandle : IFileContainerHandle

IFileHandle : IResourceHandle

---

abstract AbstractResourceHandle (protected-Methoden für alles)
final MountSpaceHandle, ... (anwendbare Methoden -> public)

---

Session:
	current user ID
	current mount space ID
	current workspace root ID

	
	
	
