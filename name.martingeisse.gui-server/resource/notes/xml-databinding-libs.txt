
Nicht brauchbar:

JAXB: codegen, kann aber auch ohne. Sieht aber nicht aus, als könnte das mit arbitrary Markup.
Apache XMLBeans: zu lowlevel
Axis Databinding: lowlevel und auf Performance ausgelegt
MOXy: JAXB implementation, s.o.
http://hmkcode.com/castor-java-object-xml -- Sieht auch so ähnlich aus, mit XML-spec, aber sehr kompliziert und ich sehe aber nichts, was dem MarkupContent entspricht.

------------------------------------------------------------------------------------------

JIBX: Mapping auf existierende Klassen mittels Mapping-Spec. Das sieht interessant aus.
	Damit lässt sich MarkupContent scheinbar umsetzen! Siehe:
		http://jibx.sourceforge.net/tutorial/binding-custom.html
	Wäre cool, aber die bekommen den Java 8 support nicht auf die Reihe.

JAXB nochmal ansehen -- alle anderen Bindings werden scheinbar deshalb vernachlässigt.
	Vielleicht geht das mit viel Aufwand. Aber eigentlich will ich ein Binding, welches
	auf Annotations aufbaut, nicht auf einem Schema-Compiler. Und selbst wenn man einen
	Custom-Parser für MarkupContent einbauen kann, will JAXB außerdem, dass das im
	Schema vorkommt -- und ich habe nicht mal ein Schema.

xmappr: Sieht auch so ähnlich wie meins aus -- das ist auch interessant
	https://code.google.com/p/xmappr/
	Könnte man vermutlich auch dazu bringen, MarkupContent zu parsen.
	Diese Bibliothek ist super veraltet!

jackson / XML-support
	"Mixed content is also out of scope, explicitly"

------------------------------------------------------------------------------------------

